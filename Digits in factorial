#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n;
    cin >> n;

    double digits = 0;
    for(int i=1;i<=n;i++)
        digits += log10(i);

    cout << (int)digits + 1;
    return 0;
}
